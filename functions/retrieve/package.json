{
  "name": "retrieve",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "yarn clean; yarn install --prod && zip -r retrieve.zip .",
    "clean": "rm -rf node_modules; rm retrieve.zip"
  },
  "license": "MIT",
  "devDependencies": {
    "aws-sdk": "^2.903.0"
  },
  "dependencies": {
    "image-type": "^4.1.0"
  }
}
